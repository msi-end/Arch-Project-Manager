ALTER TABLE `expenses` ADD `category` INT NULL DEFAULT NULL AFTER `amount`; 
ALTER TABLE `expenses` ADD FOREIGN KEY (`category`) REFERENCES `expense_category`(`id`) ON DELETE CASCADE ON UPDATE CASCADE; 